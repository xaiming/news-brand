# AI 应用开发需要掌握哪些内容

这份路线适合已经开始学习 Python、FastAPI，并且想继续往 AI 应用开发方向走的人。AI 应用开发不只是“会调用大模型接口”，更重要的是能把模型能力接入真实业务，做成稳定、可维护、可上线的产品。

## 1. 编程基础

### Python 基础

需要掌握：

- 变量、函数、类、模块
- 列表、字典、元组、集合
- 异常处理
- 文件读写
- 虚拟环境
- 包管理：`pip`、`requirements.txt`
- 类型提示：`str`、`int`、`list`、`dict`、`Optional`

AI 应用开发里，Python 是最常用的后端语言。调用模型、处理文本、操作向量数据库、写接口，大部分都可以用 Python 完成。

### 异步编程

需要理解：

- `async def`
- `await`
- 异步请求
- 异步数据库操作

FastAPI 和很多模型 API 调用都适合使用异步方式。比如多个用户同时请求 AI 接口时，异步可以提升并发能力。

## 2. Web 后端开发

### FastAPI

需要掌握：

- 路由：`GET`、`POST`、`PUT`、`DELETE`
- 请求体：Pydantic 模型
- 参数校验：`Field`
- 响应模型
- 依赖注入：`Depends`
- 统一响应格式
- 异常处理：`HTTPException`
- CORS 配置
- 接口文档：Swagger UI

你现在学的用户注册、登录、更新资料、修改密码，就属于这一部分。

### 项目分层

推荐掌握这种结构：

```text
project/
  main.py
  routers/
  schemas/
  models/
  crud/
  utils/
  config/
```

常见职责：

- `routers`：接口入口
- `schemas`：请求和响应数据结构
- `models`：数据库表模型
- `crud`：数据库操作
- `utils`：工具函数
- `config`：项目配置

AI 应用后端也需要这种分层，不然后期很容易写乱。

## 3. 数据库能力

### 关系型数据库

需要掌握：

- MySQL 或 PostgreSQL
- 表设计
- 主键、外键、索引
- 一对一、一对多、多对多
- 基础 SQL
- SQLAlchemy ORM
- 异步数据库连接

AI 应用里通常也需要保存：

- 用户信息
- 聊天记录
- 文件信息
- 知识库信息
- 任务状态
- 调用日志

### 数据库迁移

建议学习：

- Alembic
- 数据表版本管理
- 字段新增、删除、修改

项目上线后不能随便手动改数据库，迁移工具很重要。

## 4. 登录认证与权限

需要掌握：

- 密码加密：bcrypt
- 登录认证
- Token 机制
- JWT
- 权限控制
- 登录状态校验
- 接口鉴权

AI 应用通常会有：

- 普通用户
- 管理员
- 不同套餐权限
- 不同知识库权限
- API 调用额度限制

所以认证和权限是必须掌握的。

## 5. 大模型 API 调用

### 基础调用

需要掌握：

- 如何调用 LLM API
- API Key 管理
- 请求参数
- 模型选择
- system / user / assistant 消息结构
- temperature
- max tokens
- streaming 流式输出

最基础的 AI 应用通常就是：

用户输入问题 -> 后端调用大模型 -> 返回回答

### Prompt 工程

需要掌握：

- 角色设定
- 任务描述
- 输出格式约束
- 示例提示
- 多轮对话上下文
- 降低幻觉的方法
- JSON 格式输出

Prompt 不是简单写一句“你是一个助手”，而是要让模型稳定完成任务。

## 6. RAG 检索增强生成

RAG 是 AI 应用开发非常重要的一块，适合做知识库问答、企业文档助手、客服机器人。

需要掌握：

- 文档上传
- 文档解析
- 文本切分 chunk
- Embedding 向量化
- 向量数据库
- 相似度搜索
- Top-K 检索
- 把检索结果塞进 Prompt
- 让模型基于资料回答

常见流程：

```text
上传文档
  -> 提取文本
  -> 文本切分
  -> 生成 embedding
  -> 存入向量数据库
  -> 用户提问
  -> 检索相关片段
  -> 拼接 prompt
  -> 调用大模型
  -> 返回答案
```

需要了解的向量数据库：

- Chroma
- Milvus
- Qdrant
- Pinecone
- PostgreSQL + pgvector

## 7. Agent 开发

Agent 可以理解为“会调用工具、会分步骤完成任务的 AI”。

需要掌握：

- 工具调用
- 函数调用
- 任务拆解
- 多轮推理
- 工具执行结果回传给模型
- 失败重试
- 权限控制
- 防止模型乱调用工具

常见 Agent 场景：

- 自动查数据库
- 自动生成报表
- 自动调用搜索接口
- 自动写代码
- 自动操作业务系统

学习 Agent 时要注意：真实项目里最重要的不是“看起来聪明”，而是“可控、可回溯、可失败恢复”。

## 8. 文件处理能力

AI 应用经常要处理文件。

需要掌握：

- PDF 解析
- Word 文档解析
- Excel 解析
- Markdown 解析
- HTML 清洗
- 图片上传
- 文件存储
- 文件大小限制
- 文件类型校验

常见工具：

- `python-docx`
- `openpyxl`
- `pypdf`
- `pandas`
- `beautifulsoup4`

## 9. 前端基础

如果你想独立做完整 AI 应用，前端也需要会一些。

需要掌握：

- HTML
- CSS
- JavaScript
- Vue 或 React
- 表单提交
- 文件上传
- Markdown 渲染
- 流式输出展示
- 聊天界面
- 登录状态保存

AI 应用常见前端功能：

- Chat 页面
- 历史会话
- 知识库管理
- 文件上传
- 用户登录
- 管理后台

## 10. 工程化能力

### 配置管理

需要掌握：

- `.env`
- 环境变量
- 开发环境和生产环境区分
- API Key 不写死在代码里

### 日志

需要记录：

- 用户请求
- 模型调用参数
- 模型返回结果
- 错误信息
- 耗时
- token 消耗

### 错误处理

需要考虑：

- 模型接口超时
- API Key 无效
- 用户输入过长
- 文档解析失败
- 向量库连接失败
- 数据库异常

## 11. 部署能力

需要掌握：

- Linux 基础命令
- Uvicorn
- Gunicorn
- Nginx
- Docker
- Docker Compose
- HTTPS
- 域名配置
- 日志查看
- 服务重启

AI 应用上线后，还要关注：

- 接口超时
- 并发请求
- 模型费用
- 用户额度
- 数据安全

## 12. 安全意识

AI 应用尤其要注意安全。

需要掌握：

- API Key 保护
- 用户密码加密
- SQL 注入防护
- 文件上传限制
- 权限校验
- Prompt Injection 防护
- 敏感信息过滤
- 用户数据隔离

比如知识库应用里，A 用户不能看到 B 用户上传的文档。

## 13. 成本控制

调用大模型通常是有成本的。

需要掌握：

- token 计算
- 限制输入长度
- 控制上下文长度
- 缓存常见问题
- 设置用户调用额度
- 记录每次模型调用成本
- 根据任务选择合适模型

不是所有任务都要用最强模型。简单分类、摘要、格式转换可以用更便宜更快的模型。

## 14. 推荐练习项目

### 初级项目

- AI 聊天机器人
- AI 翻译工具
- AI 总结文章
- AI 起标题工具
- AI 生成日报周报

### 中级项目

- 个人知识库问答
- PDF 文档问答
- AI 客服机器人
- AI 简历优化工具
- AI SQL 查询助手

### 进阶项目

- 多知识库权限系统
- 企业内部文档助手
- Agent 自动报表系统
- AI 工作流平台
- 多模型路由系统

## 15. 推荐学习顺序

建议按这个顺序来：

```text
Python 基础
  -> FastAPI
  -> 数据库 SQLAlchemy
  -> 登录认证
  -> 大模型 API 调用
  -> Prompt 工程
  -> 流式输出
  -> RAG 知识库
  -> Agent 工具调用
  -> 部署上线
  -> 测试和工程化
```

## 16. 你当前项目对应的能力

你现在这个 FastAPI 项目已经覆盖了 AI 应用开发的后端基础：

- 路由
- 请求模型
- 响应模型
- 数据库模型
- CRUD 分层
- 用户注册
- 用户登录
- token 验证
- 用户资料更新
- 修改密码
- 统一响应

接下来最适合继续加的功能是：

- 聊天接口
- 调用大模型 API
- 流式返回
- 保存聊天记录
- 文件上传
- 简单知识库问答

## 17. 最终目标

如果你能独立完成下面这个项目，说明你已经真正入门 AI 应用开发：

```text
用户登录注册
  -> 创建知识库
  -> 上传 PDF/Word/Markdown
  -> 文档解析和切分
  -> 生成向量
  -> 用户提问
  -> 检索相关资料
  -> 调用大模型回答
  -> 保存聊天记录
  -> 前端展示流式回答
  -> 部署上线
```

做到这一步，你就不只是“会调 AI 接口”，而是能做完整 AI 产品的后端核心能力了。
